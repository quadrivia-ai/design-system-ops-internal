---
description: Run the full diagnostic agent — deep audit chain + cross-skill synthesis (proposal only).
argument-hint: "[repo path]"
---
Run the **full-system-diagnostic-agent** on $ARGUMENTS (default: current repo). Honour the small-system gate (<~5 components → recommend a single skill). Synthesise across token/component/naming/drift/health with corroboration counts. Proposal only — no changes.
