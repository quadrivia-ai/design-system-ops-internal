---
description: Detect system-wide visual/behavioural/API/token drift and classify each by cause.
argument-hint: "[repo path or area]"
---
Run the **drift-detection** skill on $ARGUMENTS (default: current repo). Classify every drift (intentional / version-lag / accidental / misunderstanding / system-gap); separate out the system-gaps as the system's backlog; labels only, scope note.
